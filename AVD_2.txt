# FastAPI and web framework dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Database drivers
pyodbc==5.0.1
pypyodbc==1.3.6

# OpenAI API
openai==1.3.7

# Environment and configuration
python-dotenv==1.0.0

# Additional utilities
python-multipart==0.0.6  # For form data handling if needed
httpx==0.25.2  # For HTTP client functionality if needed

# Development dependencies (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
flake8==6.1.0

============================================================

# Environment Configuration
ENVIRONMENT=production

# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Production Database Configuration (Kerberos Authentication)
MSSQL_PROD_SERVER=AS2480DXBDB05
MSSQL_PROD_PORT=1433
MSSQL_PROD_DATABASE=gochat247

# Alternative: SQL Server Authentication (if needed instead of Kerberos)
# Uncomment these lines if you want to use SQL Server authentication
# MSSQL_USERNAME=your_database_username
# MSSQL_PASSWORD=your_database_password

# Local Development Database Configuration (fallback)
# These are used when ENVIRONMENT is set to "local"
MSSQL_SERVER=Mohamed
MSSQL_DATABASE=gochat247

# Staging Database Configuration (if needed)
# These are used when ENVIRONMENT is set to "staging"
MSSQL_STAGING_SERVER=staging-server
MSSQL_STAGING_PORT=1433
MSSQL_STAGING_DATABASE=gochat247

# FastAPI Configuration (optional)
# FASTAPI_HOST=0.0.0.0
# FASTAPI_PORT=8000
# FASTAPI_DEBUG=false

# Logging Configuration (optional)
# LOG_LEVEL=INFO
# LOG_FORMAT=json
